https://github.com/vishakhajamkhedkar18/Habit_Tracker_Project

git commands for push and commit code to git

git init
git add .
git commit -m "<comment>"

Connect to GitHub:

git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main

PostgreSQL - password - Vishu
	     port - 5433


http://localhost:8080/api/health
http://localhost:8080/api/test-db


A habit has:

Name (e.g., “Drink Water”)

Start date

Current streak

Last completed date

Belongs to a user

We will calculate streak automatically, not manually.




---- important streak calculating logic ----

if (habit.getLastCompletedDate() == null) {
            habit.setCurrentStreak(1);
        } else if (habit.getLastCompletedDate().plusDays(1).equals(today)) {
            habit.setCurrentStreak(habit.getCurrentStreak() + 1);
        } else if (!habit.getLastCompletedDate().equals(today)) {
            habit.setCurrentStreak(1);
        } 




------ react ------

npm install axios ---> connect react to backend


/api/users/getUser/{userid}














✅ Backend Improvements

Clean API structure

Validation & proper error handling

DTOs (no direct entity exposure)

Pagination & sorting

Soft deletes

Meaningful API responses

✅ Frontend Improvements

Proper screens (Auth, Dashboard)

Basic styling

Loading & error states

Reusable components

✅ Product Features (Creative Part)

Daily habit reminder logic (backend)

Streak break detection

Progress analytics

Motivation messages

Habit categories
